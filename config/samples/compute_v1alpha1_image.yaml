# Most real usage should prefer referencing an already-prepared image via
# spec.existingId instead of creating one - see the "Images" section in
# README.md for why (image bytes must be uploaded out-of-band).
apiVersion: compute.sostackit.dev/v1alpha1
kind: Image
metadata:
  name: ubuntu-22-04
spec:
  projectId: 11111111-1111-1111-1111-111111111111
  region: eu01
  existingId: 22222222-2222-2222-2222-222222222222
